// Re-exports for shared hooks
// Placeholders: keeps import surface stable and makes discoverability easy:
export { default as useToggle } from './useToggle'
export { default as usePagination } from './usePagination'

// Add more shared hooks here and classify them under subfolders if needed (ui/, data/, app/)

export default {}
