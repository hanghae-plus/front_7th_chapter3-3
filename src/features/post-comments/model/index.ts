// Model index for the `comments` slice.
// Place comment-specific model helpers here, for example:
// export { default as normalizeComments } from './normalizeComments'

// Currently no helpers implemented. Add named exports here as needed.

export { default as useComments } from "./useComments"
